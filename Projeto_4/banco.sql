create database projeto;
use projeto;

create table cadastro(
	nome varchar(200),
    cpf varchar(14),
    telefone varchar(20),
    email varchar(200),
    nome_use varchar(200),
    senha varchar(200)
);

select * from cadastro;
